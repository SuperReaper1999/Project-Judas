build/
*.o
*.obj
# Milestone 9: *.obj above is meant for stray compiler object files (an
# MSVC-style artifact this Linux/GCC toolchain doesn't actually produce),
# not this project's own imported 3D models — un-ignore the one place
# real .obj model assets live.
!assets/**/*.obj
compile_commands.json
.cache/

# Internal session-continuity notes (not part of the public deliverable) —
# see the file itself for what it's for.
Project_Persistent_Memory.md

# Milestone 29: runtime world-state deltas written by F6 (saves/<scene>.judasstate)
saves/
